import { NextResponse } from "next/server";

function text(payload:any){return payload.output_text||payload.output?.flatMap((i:any)=>i.content||[]).map((p:any)=>p.text||"").join("")||""}
export async function POST(request:Request){try{if(!process.env.OPENAI_API_KEY)return NextResponse.json({error:"OPENAI_API_KEY is missing."},{status:400});const{focus}=await request.json();const response=await fetch("https://api.openai.com/v1/responses",{method:"POST",headers:{"Content-Type":"application/json",Authorization:`Bearer ${process.env.OPENAI_API_KEY}`},body:JSON.stringify({model:process.env.OPENAI_BLOG_MODEL||"gpt-5-mini",tools:[{type:"web_search",search_context_size:"medium"}],input:`Research current international B2B interest around ${focus||"Himalayan pink salt"}. Return JSON only: {"topics":[10 concise, factual, non-medical blog titles suitable for importers, retailers, food manufacturers and private-label brands]}. Avoid duplicate or sensational topics.`,text:{format:{type:"json_object"}}})});const payload=await response.json();if(!response.ok)return NextResponse.json({error:payload?.error?.message||"Topic research failed"},{status:response.status});const parsed=JSON.parse(text(payload).replace(/^```json\s*/i,"").replace(/```$/i,"").trim());return NextResponse.json({topics:Array.isArray(parsed.topics)?parsed.topics:[]})}catch(error){return NextResponse.json({error:error instanceof Error?error.message:"Topic research failed"},{status:500})}}
