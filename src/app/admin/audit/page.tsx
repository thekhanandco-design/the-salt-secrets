"use client";
import{useEffect,useState}from'react';import AdminShell from'@/components/admin/AdminShell';import{EnterpriseHeader,EnterpriseStyles,Panel,Badge}from'@/components/admin/EnterpriseUI';import{supabase}from'@/lib/supabase-client';import{Download,History}from'lucide-react';
export default function Page(){const[rows,setRows]=useState<any[]>([]);useEffect(()=>{void(async()=>{const{data}=await supabase.from('audit_logs').select('*').order('created_at',{ascending:false}).limit(200);setRows(data||[])})()},[]);return <AdminShell><div className="ent-page"><EnterpriseHeader eyebrow="Security accountability" title="Audit Logs" copy="Immutable activity history for user actions, content changes, exports, approvals and administrative operations." actions={<button className="ent-btn soft"><Download/>Export CSV</button>}/><Panel title="Activity history" copy="Newest actions first"><table className="ent-table"><thead><tr><th>Time</th><th>User</th><th>Action</th><th>Entity</th><th>Record</th></tr></thead><tbody>{rows.length?rows.map(x=><tr key={x.id}><td>{new Date(x.created_at).toLocaleString()}</td><td><strong>{x.actor_email||'System'}</strong></td><td><Badge tone="pink">{x.action}</Badge></td><td>{x.entity_type||'—'}</td><td>{x.entity_id||'—'}</td></tr>):<tr><td colSpan={5}>No audit events recorded yet. The V12 schema includes the audit-log table for future writes.</td></tr>}</tbody></table></Panel><div className="ent-note"><History/> Audit logging is database-ready. Sensitive values such as passwords, secrets and API tokens should never be stored in log payloads.</div><EnterpriseStyles/></div></AdminShell>}
