"use client";
import { useEffect, useState } from "react";
import AdminShell from "@/components/admin/AdminShell";
import { supabase } from "@/lib/supabase-client";
import { ShieldCheck, Users } from "lucide-react";

type Role={id:string;name:string;description:string;permissions:Record<string,boolean>};
export default function WorkflowPage(){const[roles,setRoles]=useState<Role[]>([]);useEffect(()=>{void load()},[]);async function load(){const{data}=await supabase.from("cms_roles").select("*").order("name");setRoles((data as Role[])||[])}return <AdminShell><div className="space-y-6"><div><p className="uppercase tracking-[4px] text-blue-400 font-black text-xs">Security & Workflow</p><h1 className="text-4xl font-black mt-2">Roles & Approval Rules</h1><p className="text-slate-400 mt-2">A production-ready role model for administrators, editors, writers and sales staff.</p></div><div className="grid md:grid-cols-2 xl:grid-cols-3 gap-4">{roles.map(role=><article key={role.id} className="cms-panel rounded-2xl border border-white/10 bg-[#0b1728] p-5"><div className="flex items-center gap-3"><div className="h-11 w-11 rounded-xl bg-violet-600/15 flex items-center justify-center"><ShieldCheck className="h-5 w-5 text-violet-400"/></div><div><h2 className="font-black capitalize">{role.name.replaceAll("_"," ")}</h2><p className="text-xs text-slate-500">{role.description}</p></div></div><div className="mt-4 flex flex-wrap gap-2">{Object.entries(role.permissions||{}).filter(([,v])=>v).map(([k])=><span key={k} className="rounded-full bg-blue-600/15 border border-blue-500/20 px-3 py-1 text-xs text-blue-300">{k}</span>)}</div></article>)}</div><div className="cms-panel rounded-2xl border border-white/10 bg-[#0b1728] p-5"><div className="flex items-center gap-3"><Users className="text-blue-400"/><h2 className="font-black">Recommended workflow</h2></div><div className="mt-4 grid md:grid-cols-4 gap-3 text-sm"><div className="rounded-xl bg-white/5 p-4"><b>1. Draft</b><p className="text-slate-400 mt-1">Writer creates content.</p></div><div className="rounded-xl bg-white/5 p-4"><b>2. Review</b><p className="text-slate-400 mt-1">Editor verifies accuracy and SEO.</p></div><div className="rounded-xl bg-white/5 p-4"><b>3. Approve</b><p className="text-slate-400 mt-1">Admin approves the final version.</p></div><div className="rounded-xl bg-white/5 p-4"><b>4. Publish</b><p className="text-slate-400 mt-1">Content becomes visible online.</p></div></div></div></div></AdminShell>}
